# Member repos — each is its own git repo (cloned or symlinked in), tracked
# there, never committed to the workspace repo.
/mothership
/ground-control

# Mothership runtime state
.worktrees
.mothership/

# Brainstorm visual-companion scratch + tailnet endpoint record
.superpowers/
.tailnet-vc.json

.DS_Store

# Generated task export bundles.
/*-export/
